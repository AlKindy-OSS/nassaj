# STYLE_LOCK — قفل الاتجاه الفني لنسّاج

> **حالة هذا الملف: مسوّدة تنتظر اعتماد المالك** (`approved_by` فارغ). القفل غير المعتمد مرجعٌ
> استئناسي لا مُلزِم، بحسب skill `taste-lock`.
>
> **ما الذي فعله هذا الملف ولم يفعله:** هو **توثيق لما هو منفَّذ ومقيس اليوم**، لا اتجاه فنّي جديد.
> أُنشئ بقرار المالك 2026-07-31 بخيار «اشتقّه من الموجود فقط» بعد إعادة تصميم الويكي (T-1094). كل
> قيمة أدناه مأخوذة من الكود أو من قياس فعلي، ومرجعها مذكور. لم يُخترع لون ولا خط ولا بذرة حركة.
>
> **تغيير أي قيمة مقفلة = قرار مالك صريح + رفع `version` + سطر في سجل التغيير (§7).**

---

## 0) الكتلة الحتمية (تُقرأ آلياً)

```yaml
project:      nassaj-dev
locked_at:    2026-07-31
approved_by:  <بانتظار اعتماد المالك>
version:      1
derived_from: منفَّذ قائم — لا اتجاه جديد

typography:
  arabic_ui:        IBM Plex Sans Arabic     # src/lib/ui-font.ts — DEFAULT_UI_FONT
  arabic_display:   IBM Plex Sans Arabic     # لا طبقة عناوين منفصلة اليوم (انظر §1)
  latin_ui:         IBM Plex Sans            # نفس العائلة الفائقة — تالي في stack العربي
  latin_display:    IBM Plex Sans
  base_size_px:          16
  arabic_size_boost_px:  0        # غير مطبَّق اليوم — دَين مرصود في §1
  line_height_body:      1.75
  line_height_heading:   1.35
  letter_spacing_arabic: 0        # مفروض كوداً — أي قيمة ≠ 0 فشل حتمي
  numerals:         western       # مفروض باختبار wikiNumerals.test.ts

palette_model:    hsl-derived-presets         # ليست OKLCH — انظر §2 «انحراف معلن»
palette_source:   src/lib/theme-presets.ts + src/index.css
presets:          [default, claude, alkindy, cursor, codex, gemini, custom]
default_preset:   alkindy                     # loadThemePresetState()

shape:
  radius_base:   0.5rem          # --radius في src/index.css
  personality:   soft

density:         compact
spacing_base_px: 4               # مقياس Tailwind الافتراضي

motion:
  seed:              Snap        # 120ms للألوان/النص، 220ms للتخطيط — مقروء من wiki-panel.css
  duration_fast_s:   0.12
  duration_normal_s: 0.22
  easing:            [0.2, 0.6, 0.3, 1]
  reduced_motion:    required    # prefers-reduced-motion مطبَّق (wiki-panel.css:710)
  rtl_aware:         required

direction_root: html             # RtlContext.jsx:58 — documentElement.dir
```

---

## 1) الطباعة

**الطبقات المنفَّذة اليوم طبقة واحدة لا ثلاث.** IBM Plex Sans Arabic يخدم الواجهة والعناوين معاً،
وهذا **انحراف معلن** عن قاعدة «ثلاث طبقات لا تُخلط» في `arabic-rtl-excellence`.

سبب الاختيار (من `src/lib/ui-font.ts`، مقفل بالتعليق هناك): تبعية قائمة أصلاً بلا حزمة جديدة، وتحمل
وزن 600 حقيقياً فلا يُصطنَع، وهي عائلة نظام التصميم الحكومي السعودي (DGA)، وتقترن بـIBM Plex Sans من
العائلة الفائقة نفسها فيبقى للسطر المختلط إيقاع واحد.

الخطوط الخمسة الأخرى (`noto-sans-arabic`, `readex-pro`, `vazirmatn`, `tajawal`, `system`) خيارات
مستخدم في الإعدادات، **لا بدائل يختارها وكيل** — تُحمَّل عند الطلب فقط.

**محظورات حتمية (تُسقِط المراجعة):**
- `letter-spacing` على نص عربي متصل — صفر دائماً.
- ALL-CAPS على العربية، ونمط eyebrow اللاتيني (uppercase + tracking) — **هذا بالضبط جذر B-329**:
  استُنسخ الشكل بعد نزع أداتَي عمله فانهار التسلسل.
- خلط `0123` مع `٠١٢٣` على شاشة واحدة. الكود والمسارات والإصدارات مستثناة داخل `code`.
- Inter/Roboto/Open Sans خطَّ جسم.
- `Tajawal` داخل المخطّطات (نُظِّف 58 وروداً في B-329 وعاد إلى صفر).
- لصيقة نصية دون 13px (نُظِّف 36 وروداً في B-329).

**دَين مرصود:** `arabic_size_boost_px: 0` — لا زيادة حجم للعربي مقابل مكافئه اللاتيني، خلافاً
لتوصية المهارة. لم يُلمس لأن أثره عامّ على كل الشاشات.

---

## 2) اللوحة

**انحراف معلن عن القالب:** القالب يفترض لوحة OKLCH واحدة داخل `@theme` (Tailwind v4). نسّاج على
Tailwind 3 ويستعمل ثلاثيات HSL خامّة (`--token: H S% L%`) تُستهلك `hsl(var(--token))`. وأهمّ من ذلك:
**لا توجد لوحة واحدة تُقفل** — هناك محرّك اشتقاق (`deriveTokens`) يولّد الرموز من لون علامة واحد
لستة بريستات × وضعين. لذلك المقفول هنا **الثوابت**، لا القيم.

### الثوابت المقفلة

1. **الرموز الدلالية حصراً.** ممنوع لون خام (`text-white`, `bg-black`, hex, `amber-500`) داخل مكوّن
   جديد. الرموز في `src/index.css` و`theme-presets.ts` هي المصدر.
2. **الحدود من `--border`.** الحدّ المشتقّ من `--foreground` محظور: في الوضع الداكن `--foreground`
   أبيضُ تقريباً فينتج هالة بيضاء لا ظلاً (B-331). يحرسه `wikiThemeAlignment.test.ts`.
3. **الظلال سوداء** برفع الشفافية — لا مشتقّة من `--foreground`، للسبب نفسه.
4. **حلقة التركيز `--ring` حصراً**، وهي مضمونة ≥ 3:1 مقابل **خمسة** أسطح
   (`background`, `card`, `popover`, `muted`, `secondary`) في البريستات الستة والوضعين.
   يحرسه `src/lib/themeRingContrast.test.ts` (30 تركيبة). القياس مقابل `--background` وحده هو ما
   أخفى العيب أصلاً: alkindy فاتح كان 2.86 على الخلفية و**2.63** على muted.
5. **WCAG 1.4.11 لا يُوسَّع.** المعيار يغطّي «ما يلزم لتحديد المكوّنات وحالاتها» — لا حدّاً حول كل
   شيء. توسيعه في B-121 إلى خلايا الجداول والفواصل والألواح هو ما أنتج «التباين الصارخ»، والعلاج
   كان **تقليص المُلزَمين** لا خفض القيمة (B-331).
6. **قاعدة الرأس ومرؤوسه** (B-329، مُلزِمة): الرأس ليس أصغر من مرؤوسه، ويختلف عنه بمحورين مستقلين
   على الأقل، و`--muted-foreground` للمرؤوسين وحدهم. يحرسها `wikiHeadingHierarchy.test.ts`.

### البريست الافتراضي

`alkindy` (ذهبي/كحلي) — هوية دار الكِندِي، ومرجعها `AlKindy-Brand-Book.html`. ليس بنفسجياً
افتراضياً، فلا ينطبق محظور «بصمة الـslop» في القالب.

**دَين مرصود:** البريستات الستة قِيست آلياً على محور حلقة التركيز فقط (30 تركيبة). بقيّة المحاور
البصرية قِيست بصرياً على `alkindy` وحده في الوضعين — البريستات الخمسة الأخرى تتبع الرموز نظرياً،
وهي حجّة لا قياس.

---

## 3) الاستدارة والكثافة

استدارة `0.5rem` (`soft`) · كثافة `compact` (أداة تشغيلية يومية لا صفحة هبوط) · إيقاع مضاعفات 4px.

---

## 4) الحركة

بذرة `Snap`: 120ms للون/النص، 220ms للتخطيط (عرض/إزاحة)، `cubic-bezier(0.2, 0.6, 0.3, 1)`.
`prefers-reduced-motion` يُلغي انتقالات الأدراج تماماً لا يُبطئها.

---

## 5) RTL

- `dir` على `<html>` عبر `RtlContext` — لا على `<body>`.
- **أساس اتجاه واحد لكل رسالة**، لا `dir="auto"` لكل كتلة: كسر ترتيب الرسائل العربية (B-207).
- **لصيقة بلا `dir`، وجملة تأخذ `dir` من لغة نصّها.** الحظر الشامل لـ`dir` حماية زائدة أنتجت عطباً:
  أربع جمل إنجليزية ورثت أساس rtl فهبطت نقاطها إلى بداية السطر. `unicode-bidi: isolate` يعزل ولا
  يمنح أساساً (B-331).
- **الإزاحة المنطقية فخّ داخل جزر `dir=ltr`:** `tailwindcss-rtl` تضبط `left` و`right` معاً فيتمدّد
  العنصر. الحلّ فيزيائي صريح، والقياس بالمتصفح لا بالقراءة.

---

## 6) القاعدة الحاكمة للمراجعة

**فحوصنا كانت مطلقة والعيوب كانت تفاضلية.** «هل اللون من النظام؟ هل يجتاز 4.5:1؟» — كل عنصر يجتاز
وحده والعطب في العلاقة بينهما. وفحص المطابقة للـBrief لا يكشف عيباً في الـBrief نفسه.

يترتّب على ذلك ثلاثة إلزامات:
1. **قِس تفاضلياً** (عنصر مقابل جاره)، لا مطلقاً وحده.
2. **قِس على كل الأسطح والبريستات**، لا على الخلفية والبريست الافتراضي.
3. **تحقّق بصرياً بالمتصفح قبل أي إعلان إنجاز.** أربع مرات في T-1094 بدا شيء سليماً في الكود
   ومكسوراً على الشاشة. والحارس الذي لا يسقط عند إعادة العيب ليس حارساً — اختبره بطفرة.

---

## 7) سجل التغيير

| version | التاريخ | التغيير | المعتمِد |
|---|---|---|---|
| 1 | 2026-07-31 | إنشاء القفل توثيقاً للمنفَّذ بعد T-1094، بلا اتجاه جديد | بانتظار المالك |
